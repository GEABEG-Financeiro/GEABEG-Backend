const moment = require('moment');

module.exports = ({ }) => ({
    execute: async (body) => {
        try {
        } catch (error) {
            console.log('create schedules - [Error]: ', error);
            throw error;
        }
    },
});
